# Verifier run — v6 Battle Animation 2.0 — 2026-07-27

Command: `python3 test_battle_fx.py` (headless chromium, swiftshader) — exit 0.

## Results against verifier/v6/acceptance.md

| # | Criterion | Result | Evidence |
|---|-----------|--------|----------|
| 1 | Per-beat Addition FX | PASS | shots/84: `1-HIT ADDITION!` + spark burst + slash arc on wolf; beatFX escalates count/color, PERFECT adds ring + FOV punch |
| 2 | Dragoon cinematic | PASS | `dragoon form active: True`; shots/81-82: feather burst + wings forming, log `The Dragoon Spirit ignites!`; pillar self-removes (no orphan additive cylinders) |
| 3 | Per-family death anims | PASS | `wolf slain + results shown: True` (crumple path); wraith/golem/humanoid branches code-reviewed, zero errors exercising wolf + golem fights |
| 4 | Kind attack sounds | PASS | growl/stomp/whoosh/swing mapped in hitTarget; new SFX registered without errors |
| 5 | Turn-cycle bug fix | PASS | probe: before fix the player NEVER got a 2nd turn with Serah-only party (`YOUR TURN — SERAH` forever); after fix cycle returns to `DRAGOON KNIGHT` every round |
| 6 | Stability | PASS | `ERRORS: (none)` |
| 7 | Regression | PASS | test_quests.py: MELBU PHASE 2 + dead, `(none)`; test_lyra.py: full 4-actor cycle + Emberbolt 6MP + Alt-equip, `(none)`; test_peaks.py: stormcaller chain, `(none)` |

## In-loop fixes this iteration
- **Critical pre-existing bug**: with no Kael/Lyra in the party, `kaelPhase()`'s fallback
  called `enemyTurn()` without resetting `currentActor` — the player was locked out of
  every turn for the rest of the battle (only Serah acted repeatedly). Fixed in
  `lyraPhase()` (the terminal ally phase): reset to `'player'` before `enemyTurn()`.
- Test needed unkillable-enemy override to survive long FX sequences; menu polling
  needed visibility + actor-name matching (turn menus are dynamic).
